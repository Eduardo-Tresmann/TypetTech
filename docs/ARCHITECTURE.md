# Arquitetura do TypeTech

Este documento descreve a arquitetura do sistema TypeTech, incluindo diagramas, fluxos de funcionamento e relacionamentos entre componentes.

## 📐 Diagrama de Arquitetura Geral

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENTE (Browser)                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │              Next.js App (React 19)                     │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │    │
│  │  │   Pages      │  │  Components  │  │    Hooks     │   │    │
│  │  │  (App Router)│  │   (React)    │  │  (Custom)    │   │    │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │    │
│  │         │                 │                 │           │    │
│  │  ┌──────▼─────────────────▼─────────────────▼───────┐   │    │
│  │  │         Core Services & Contexts                 │   │    │
│  │  │  GameService | SoundService | ThemeService       │   │    │
│  │  │  AuthContext | GameConfigContext                 │   │    │
│  │  └──────┬───────────────────────────────────────────┘   │    │
│  └─────────┼───────────────────────────────────────────────┘    │
│            │                                                    │
│            │ HTTPS/REST                                         │
└────────────┼────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SUPABASE (Backend)                         │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐   │
│  │   PostgreSQL     │  │   Authentication │  │   Storage    │   │
│  │   Database       │  │   (Auth)         │  │   (Avatares) │   │
│  └──────────────────┘  └──────────────────┘  └──────────────┘   │
│         │                      │                      │         │
│         └──────────────────────┼──────────────────────┘         │
│                                │                                │
│                    Row Level Security (RLS)                     │
└─────────────────────────────────────────────────────────────────┘
```

## 🎮 Fluxo do Jogo de Digitação

### Fluxo Principal

```
┌─────────────┐
│  Usuário    │
│  acessa     │
│  /home      │
└─────┬───────┘
      │
      ▼
┌─────────────────────────────────────┐
│  TypingScreen (page.tsx)            │
│  - Inicializa useTypingTest()       │
│  - Renderiza TypingDisplay          │
└─────┬───────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────┐
│  useTypingTest Hook                 │
│  - Gera texto aleatório             │
│  - Gerencia estado (input, tempo)   │
│  - Calcula WPM e precisão           │
│  - Monitora teclas pressionadas     │
└─────┬───────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────┐
│  TypingDisplay Component            │
│  - Exibe texto formatado            │
│  - Mostra cursor e progresso        │
│  - Captura input do usuário         │
└─────┬───────────────────────────────┘
      │
      │ [Usuário digita]
      │
      ▼
┌─────────────────────────────────────┐
│  GameService                        │
│  - Calcula estatísticas             │
│  - Valida entrada                   │
└─────┬───────────────────────────────┘
      │
      │ [Tempo acaba]
      │
      ▼
┌─────────────────────────────────────┐
│  ResultsScreen                      │
│  - Exibe WPM, precisão, erros       │
│  - Botão para reiniciar             │
└─────┬───────────────────────────────┘
      │
      │ [Salvar resultado]
      │
      ▼
┌─────────────────────────────────────┐
│  saveTypingResult()                 │
│  - Valida dados                     │
│  - Insere no Supabase               │
│  - Verifica recordes                │
└─────────────────────────────────────┘
```

### Fluxo Detalhado de Digitação

```
1. INICIALIZAÇÃO
   ┌─────────────────────────────────────┐
   │ useTypingTest() inicializa:         │
   │ - Gera texto via TextGenerator      │
   │ - Define tempo (15/30/60/120s)      │
   │ - Reseta contadores                 │
   └─────────────────────────────────────┘

2. AGUARDANDO INPUT
   ┌─────────────────────────────────────┐
   │ - Timer pausado                     │
   │ - Cursor piscando                   │
   │ - Aguardando primeira tecla         │
   └─────────────────────────────────────┘

3. DIGITANDO
   ┌─────────────────────────────────────┐
   │ A cada tecla:                       │
   │ - Valida caractere                  │
   │ - Atualiza currentIndex             │
   │ - Calcula WPM em tempo real         │
   │ - Toca som (se habilitado)          │
   │ - Atualiza visualização             │
   └─────────────────────────────────────┘

4. FINALIZAÇÃO
   ┌─────────────────────────────────────┐
   │ - Para timer                        │
   │ - Calcula estatísticas finais       │
   │ - Verifica recordes pessoais        │
   │ - Salva no banco (se autenticado)   │
   │ - Exibe ResultsScreen               │
   └─────────────────────────────────────┘
```

## 🧩 Relacionamento entre Componentes

### Camadas da Aplicação

```
┌─────────────────────────────────────────────────────────────┐
│                    CAMADA DE APRESENTAÇÃO                   │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │   Pages     │  │  Components  │  │    Layout    │        │
│  │             │  │              │  │              │        │
│  │ /home       │  │ TypingDisplay│  │   Header     │        │
│  │ /profile    │  │ ResultsScreen│  │   Footer     │        │
│  │ /friends    │  │ ModeBar      │  │              │        │
│  └──────┬──────┘  └──────┬───────┘  └──────┬───────┘        │
└─────────┼─────────────────┼─────────────────┼───────────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌────────────────────────────────────────────────────────────┐
│                    CAMADA DE LÓGICA                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │    Hooks     │  │   Contexts   │  │   Services   │      │
│  │              │  │              │  │              │      │
│  │ useTyping    │  │ AuthContext  │  │ GameService  │      │
│  │ useGameConfig│  │ GameConfig   │  │ SoundService │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
└─────────┼─────────────────┼─────────────────┼──────────────┘
          │                 │                 │
          ▼                 ▼                 ▼
┌────────────────────────────────────────────────────────────┐
│                    CAMADA DE DADOS                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Database   │  │  Supabase    │  │   Storage    │      │
│  │   Services   │  │   Client     │  │              │      │
│  │              │  │              │  │              │      │
│  │ db.ts        │  │ supabaseClien│  │ Avatares     │      │
│  │ FriendService│  │ t.ts         │  │              │      │
│  │ UserService  │  │              │  │              │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────────────────────────────────────────────┘
```

### Componentes Principais do Jogo

```
TypingScreen (page.tsx)
    │
    ├──► ModeBar
    │    └──► Seleciona tempo (15/30/60/120s)
    │
    ├──► TypingDisplay
    │    ├──► Input invisível (captura teclas)
    │    ├──► Renderização de texto formatado
    │    └──► Indicadores visuais (cursor, erros)
    │
    └──► ResultsScreen (quando finalizado)
         ├──► Exibe estatísticas
         ├──► Botão de reiniciar
         └──► Link para login (se não autenticado)

useTypingTest Hook
    │
    ├──► GameService
    │    ├──► TextGeneratorFactory
    │    │    └──► DifficultyStrategy
    │    └──► calculateStats()
    │
    ├──► SoundService
    │    └──► Toca sons de feedback
    │
    └──► saveTypingResult()
         └──► Supabase Database
```

## 🔐 Fluxo de Autenticação

```
┌─────────────────────────────────────────────────────────────┐
│                    FLUXO DE AUTENTICAÇÃO                     │
└─────────────────────────────────────────────────────────────┘

1. REGISTRO
   ┌─────────────┐
   │  Usuário    │
   │  acessa     │
   │  /auth/     │
   │  register   │
   └─────┬───────┘
         │
         ▼
   ┌─────────────────────┐
   │  AuthForm           │
   │  (mode: register)   │
   └─────┬───────────────┘
         │
         ▼
   ┌─────────────────────┐
   │  supabase.auth      │
   │  .signUp()          │
   └─────┬───────────────┘
         │
         ▼
   ┌─────────────────────┐
   │  Trigger SQL        │
   │  cria profile       │
   │  automaticamente    │
   └─────┬───────────────┘
         │
         ▼
   ┌─────────────────────┐
   │  Redireciona        │
   │  para /home         │
   └─────────────────────┘

2. LOGIN
   ┌─────────────┐
   │  Usuário    │
   │  acessa     │
   │  /auth/     │
   │  login      │
   └─────┬───────┘
         │
         ▼
   ┌─────────────────────┐
   │  AuthForm           │
   │  (mode: login)      │
   └─────┬───────────────┘
         │
         ▼
   ┌─────────────────────┐
   │  supabase.auth      │
   │  .signInWith        │
   │  Password()         │
   └─────┬───────────────┘
         │
         ▼
   ┌─────────────────────┐
   │  AuthContext        │
   │  atualiza user      │
   └─────┬───────────────┘
         │
         ▼
   ┌─────────────────────┐
   │  Redireciona        │
   │  para /home         │
   └─────────────────────┘

3. GERENCIAMENTO DE SESSÃO
   ┌─────────────────────┐
   │  AuthProvider       │
   │  (no layout.tsx)    │
   └─────┬───────────────┘
         │
         ├──► onAuthStateChange()
         │    └──► Atualiza user automaticamente
         │
         └──► getSession()
              └──► Verifica sessão ao carregar
```

## 👥 Fluxo do Sistema Social

### Sistema de Amizades

```
┌─────────────────────────────────────────────────────────────┐
│                  SISTEMA DE AMIZADES                        │
└─────────────────────────────────────────────────────────────┘

1. ENVIAR SOLICITAÇÃO
   Usuário A                    Usuário B
   ┌────────┐                  ┌─────────┐
   │ Busca  │                  │         │
   │ usuário│                  │         │
   └───┬────┘                  └─────────┘
       │
       ▼
   ┌─────────────────────┐
   │ FriendService       │
   │ .sendRequest()      │
   └───┬─────────────────┘
       │
       ▼
   ┌─────────────────────┐
   │ INSERT INTO         │
   │ friend_requests     │
   └───┬─────────────────┘
       │
       ▼
   ┌─────────────────────┐
   │ Trigger SQL         │
   │ cria notificação    │
   │ para Usuário B      │
   └─────────────────────┘

2. ACEITAR SOLICITAÇÃO
   Usuário B
   ┌────────┐
   │ Aceita │
   │ convite│
   └───┬────┘
       │
       ▼
   ┌─────────────────────┐
   │ accept_friend_      │
   │ request()           │
   └───┬─────────────────┘
       │
       ├──► INSERT INTO friends
       │
       └──► DELETE friend_request
            └──► Trigger limpa notificação
```

### Sistema de Mensagens

```
┌─────────────────────────────────────────────────────────────┐
│                  SISTEMA DE MENSAGENS                       │
└─────────────────────────────────────────────────────────────┘

Usuário A                    Usuário B
┌─────────┐                 ┌─────────┐
│ Envia   │                 │         │
│ mensagem│                 │         │
└───┬─────┘                 └─────────┘
    │
    ▼
┌─────────────────────┐
│ FriendService       │
│ .sendMessage()      │
└───┬─────────────────┘
    │
    ▼
┌─────────────────────┐
│ INSERT INTO         │
│ direct_messages     │
└───┬─────────────────┘
    │
    ▼
┌─────────────────────┐
│ Trigger SQL         │
│ cria notificação    │
│ para Usuário B      │
└─────────────────────┘
```

### Sistema de Notificações

```
┌─────────────────────────────────────────────────────────────┐
│                  SISTEMA DE NOTIFICAÇÕES                    │
└─────────────────────────────────────────────────────────────┘

Eventos que geram notificações:
    │
    ├──► Solicitação de amizade enviada
    │    └──► friend_requests_notification_trigger
    │
    ├──► Mensagem recebida
    │    └──► direct_messages_notification_trigger
    │
    ├──► Recorde superado por amigo
    │    └──► typing_results_record_beaten_trigger
    │
    └──► Solicitação resolvida (aceita/rejeitada)
         └──► friend_requests_cleanup_notification_trigger
```

## 🗄️ Estrutura de Dados

### Tabelas Principais

```
┌─────────────────────────────────────────────────────────────┐
│                    BANCO DE DADOS                           │
└─────────────────────────────────────────────────────────────┘

profiles
├── id (UUID, PK)
├── user_id (UUID, FK -> auth.users)
├── display_name (TEXT)
├── avatar_url (TEXT)
└── created_at (TIMESTAMP)

friends
├── id (UUID, PK)
├── user_a (UUID, FK -> auth.users)
├── user_b (UUID, FK -> auth.users)
└── created_at (TIMESTAMP)

friend_requests
├── id (UUID, PK)
├── sender_id (UUID, FK -> auth.users)
├── receiver_id (UUID, FK -> auth.users)
├── status (ENUM: pending, accepted, rejected, cancelled)
└── created_at (TIMESTAMP)

direct_messages
├── id (UUID, PK)
├── sender_id (UUID, FK -> auth.users)
├── receiver_id (UUID, FK -> auth.users)
├── content (TEXT)
├── pair_key (TEXT) -- Agrupa mensagens da mesma conversa
├── read_at (TIMESTAMP)
└── created_at (TIMESTAMP)

typing_results
├── id (UUID, PK)
├── user_id (UUID, FK -> auth.users)
├── total_time (INTEGER) -- 15, 30, 60, 120
├── wpm (INTEGER)
├── accuracy (INTEGER)
├── correct_letters (INTEGER)
├── incorrect_letters (INTEGER)
└── created_at (TIMESTAMP)

notifications
├── id (UUID, PK)
├── user_id (UUID, FK -> auth.users)
├── type (TEXT) -- friend_request, message, record_beaten
├── metadata (JSONB) -- Dados extras
├── read_at (TIMESTAMP)
└── created_at (TIMESTAMP)
```

### Relacionamentos

```
auth.users (Supabase Auth)
    │
    ├──► profiles (1:1)
    │
    ├──► friends (N:M via user_a e user_b)
    │
    ├──► friend_requests (1:N como sender ou receiver)
    │
    ├──► direct_messages (1:N como sender ou receiver)
    │
    ├──► typing_results (1:N)
    │
    └──► notifications (1:N)
```

## 🎨 Padrões de Design Utilizados

### 1. Factory Pattern
**Localização:** `src/core/factories/TextGeneratorFactory.ts`

```typescript
TextGeneratorFactory.create(difficulty)
    └──► RandomTextGenerator
         └──► DifficultyStrategy (Easy/Medium/Hard)
```

### 2. Strategy Pattern
**Localização:** `src/core/strategies/DifficultyStrategy.ts`

```typescript
DifficultyStrategyFactory.create(difficulty)
    ├──► EasyDifficultyStrategy
    ├──► MediumDifficultyStrategy
    └──► HardDifficultyStrategy
```

### 3. Service Layer Pattern
**Localização:** `src/core/services/`

```
GameService    - Lógica de jogo
SoundService   - Gerenciamento de sons
ThemeService   - Gerenciamento de temas
```

### 4. Context API Pattern
**Localização:** `src/context/`

```
AuthContext      - Estado de autenticação
GameConfigContext - Configurações do jogo
```

### 5. Custom Hooks Pattern
**Localização:** `src/hooks/`

```
useTyping      - Lógica do teste de digitação
useGameConfig  - Configurações do jogo
useSound       - Controle de sons
```

## 🔄 Fluxo de Dados

### Salvamento de Resultado

```
1. Usuário finaliza teste
   │
   ▼
2. useTypingTest calcula estatísticas
   │
   ▼
3. saveTypingResult() valida dados
   │
   ▼
4. Rate limiting verifica
   │
   ▼
5. INSERT INTO typing_results
   │
   ▼
6. Trigger verifica recordes
   │
   ├──► Se bateu recorde pessoal
   │    └──► Atualiza estado isNewRecord
   │
   └──► Se bateu recorde de amigo
        └──► Cria notificação
```

### Busca de Dados

```
1. Componente solicita dados
   │
   ▼
2. Service (FriendService, UserService, etc.)
   │
   ▼
3. getSupabase() obtém cliente
   │
   ▼
4. Query no Supabase
   │
   ├──► RLS verifica permissões
   │
   └──► Retorna dados filtrados
```

## 🛡️ Segurança

### Row Level Security (RLS)

Todas as tabelas principais implementam RLS:

- **profiles**: Usuários só podem atualizar seu próprio perfil
- **friends**: Usuários só veem suas próprias amizades
- **friend_requests**: Usuários só veem solicitações enviadas/recebidas
- **direct_messages**: Usuários só veem mensagens de suas conversas
- **typing_results**: Usuários só podem inserir seus próprios resultados
- **notifications**: Usuários só veem suas próprias notificações

### Rate Limiting

Implementado em `src/utils/security.ts`:
- Limita tentativas de login/registro
- Limita salvamento de resultados
- Previne abuso do sistema

## 📱 Responsividade

A aplicação é totalmente responsiva:

- **Mobile First**: Design otimizado para dispositivos móveis
- **Breakpoints Tailwind**: sm, md, lg, xl, 2xl
- **Viewport Handling**: Ajusta altura quando teclado virtual aparece
- **Touch Events**: Prevenção de scroll durante o jogo

## 🎯 Performance

### Otimizações Implementadas

1. **Lazy Loading**: Componentes carregados sob demanda
2. **Memoização**: useCallback e useMemo em hooks críticos
3. **Debouncing**: Prevenção de múltiplos eventos de teclado
4. **Virtual Scrolling**: Texto longo renderizado em chunks
5. **Code Splitting**: Next.js divide automaticamente o código

---

**Última atualização:** Novembro 2025

